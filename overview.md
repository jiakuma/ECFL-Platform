# 患者详情页：就诊级 / 事件级 层级关系重构

## 目标
理顺「患者 → 就诊 Encounter → 诊疗事件 ClinicalEvent → 多模态资源 Resource」的层级，保持「左侧诊疗时间轴（事件级）+ 右侧就诊概览（就诊级）」布局，修正概念混淆与字段语义。

## 数据层改动（`src/mock/patients.ts`）
- **`TimelineKind`**：`就诊` 改名为 **`就诊节点`**，仅表示首诊 / 入院 / 转院等真实就诊行为，作为时间轴结构性标记（不参与筛选桶）。
- **`TimelineNode`**：新增 `encounter` 字段，把每个事件级节点归属到一次就诊 id。
- **`VisitRecord`**：新增 `results`（关键结果）字段。
- **`buildEncounters()`**（替换旧 `buildVisits`）：为每条事件打上所属就诊 id；锚点规则——**就诊节点 / 手术治疗 / 随访**各自开启独立就诊，其余事件（检查 / 诊断分期 / 治疗决策 / 非手术治疗）并入最近锚点；并补齐随访记录中未在时间轴单独成节点的随访为独立就诊，保证「就诊概览」完整。
- **字段语义修正**：就诊卡片「主要诊断」= 疾病名（如「食管鳞状细胞癌」）；`术后病理 ypT2N1M0`、分期、疗效率等归入新增的「关键结果」，不再混入主要诊断。

## 组件改动（`src/views/PatientDetailView.vue`）
- **左侧筛选**：改为 `全部 / 检查 / 病理分期 / 治疗 / 随访`；`就诊节点`标记始终展示以表达就诊边界。
- **右侧就诊概览卡片**：
  - 新增 **关键结果** 行（紫色标签）；
  - 标签改名：`治疗 / 处理` → **关键诊疗**，`结论 / 转归` → **后续计划 / 转归**；
  - 卡片**可点击**：点击 → 切到就诊概览并**高亮左侧该次就诊期间的全部事件**（短暂闪烁 + 滚动定位）。
- **双向联动**：点击左侧任一事件 → 同步高亮其所属就诊卡片；点击左侧就诊节点 → 定位该次就诊。
- 视觉沿用明亮医疗科技风，未新增可信数据空间展示。

## 验证
- `vue-tsc` 类型检查 0 错误；`npm run build` 0 错误。
- 开发预览：`http://localhost:5175/`（示例 `#/dataset/patients/P-0001`）。

## 未改动
完整档案、多模态资源及其他业务页面均未修改；列表页主索引号 `PMI-EC-` 与详情页 `EC-2024-` 格式不一致仍保留，如需统一可继续。
